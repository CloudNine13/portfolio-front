const headerStyle =
  'md:text-9xl text-6xl font-display font-black md:leading-23 leading-11 uppercase';
const heroContainerStyle =
  'dot-bg w-full flex flex-col md:flex-row items-center justify-center border-b-4 border-black md:gap-20 gap-7 md:h-[100vh] h-fit py-15 md:py-0';
const textContainerStyle = 'flex flex-col md:gap-13 gap-5 pt-5 md:pt-0';
const imageContainerStyle = 'relative flex flex-col md:gap-3 gap-2 items-center justify-center';
const descriptionStyle = 'max-w-xs md:max-w-xs lg:max-w-sm text-justify font-sans text-gray';
const highlightStyle = 'text-yellow bg-black py-4 px-6 italic';
const nameStyle = 'text-3xl font-display tracking-tighter';
const overlayIconStyle =
  'absolute -top-5 -right-5 bg-red text-white z-10 w-12 h-12 flex items-center justify-center border-4 border-black';
const buttonContainerStyle = 'flex flex-col md:flex-row gap-5 items-center';
const buttonStyle = 'min-w-75 md:min-w-fit min-h-12 md:min-h-fit disabled:cursor-default';

export {
  headerStyle,
  heroContainerStyle,
  textContainerStyle,
  imageContainerStyle,
  descriptionStyle,
  highlightStyle,
  nameStyle,
  overlayIconStyle,
  buttonContainerStyle,
  buttonStyle,
};
